extends base_crud
block content
   div.row
      div.col
         h1.border-bottom.mb-4=title
   div.row(style="margin:auto")
      div.col
            a(href="/") Topに戻る
   div.row
      div.col-md-6
         a.btn.btn-primary.btn-sm.my-3(href='./accounts/create') 新規登録
      div.container-fluid 
         div.row  
            table.table.table-bordered.table-striped
               thead
               - var names=['ID','名前','性別','変更']
               each name in names
                  th(scope="col").text-center=name
            tbody
               each data in accounts
               tr
                  td(scope="row") #{data.id}
                  td: a(href="./accounts/detail/#{data.id}") #{data.first_name}+' '+#{data.last_name}
                  td: case #{data.gender}
                     when 1
                        p 男性
                     default
                        p 女性
                  td
                     a(href="/accounts/update/#{data.id}" style="width:20%; margin-right:5%;").btn.btn-success.btn-sm 更新
                     a(href='/accounts/delete/#{data.id}' style="width:20%; margin-right:5%;").btn.btn-danger.btn-sm 削除
            //・削除ボタン：アラート出してOKしたら削除する方向で！
